# 代理开发规范

这份文件是**跨项目通用**的工作规范，可以原样复制到任何仓库。项目专属的内容——技术栈、代码在哪、哪些边界不能碰、怎么跑验证——一律放在同目录的 `PROJECT.md`。

**动手之前先读 `PROJECT.md`。** 这里只讲「怎么做事」，那里才讲「这个项目是什么样」。

## 交流

- 分析、说明、代码注释和 Git 提交信息一律用简体中文；代码标识符保持仓库现有的英文风格。
- 注释写「为什么」，不写「是什么」。能从代码一眼看出来的事不必复述，值得写的是当初踩过的坑和不能这么改的理由。
- 报告只列真正做过、真正验证过的。没跑的检查不能写成已通过，跳过的测试不能写成已覆盖。

## 修改

- 在现有架构上小步改，优先复用已有的服务和模块，不为「架构更漂亮」另建一套同类实现。
- 未经明确要求，不重构核心领域模型，不扩展业务范围，不顺手升级依赖、引入框架或批量改换行符。
- 动手前先 `git status --short`：工作区可能已经有用户自己的改动，不得覆盖、回滚或格式化无关文件。
- 用补丁方式改文件，不要整文件重写——重写会把别人刚加的东西冲掉，diff 也没法看。
- 凭据只从环境变量来。任何真实的数据库密码、对象存储密钥、第三方 API Key 都不进仓库。

## 定位代码

- 先查 `PROJECT.md` 的定位表，再用 `rg` 搜关键字；不要漫无目的地遍历目录。
- 永远跳过依赖和产物目录：`node_modules/`、`target/`、`dist/`、`build/`、测试报告目录。
- 拿不准某个 API 或 CSS 属性的真实行为时**当场验一次**，不要凭记忆写方案——写几十行的临时脚本跑一遍，比返工便宜得多。

## 验证与交付

改了哪一层就跑哪一层的验证，交付前再跑一遍项目定义的完整入口（具体命令见 `PROJECT.md`）。下面几条是反复踩出来的，任何项目都适用：

- **改了 UI 就必须真跑一次端到端测试。** 单测和类型检查发现不了「测试还在点一个已经不存在的元素」，也发现不了「滚动容器换了一层，于是那句 `scrollTop` 什么也没做」。
- **跳过等于没测。** 缺少 Docker、缺少数据库、缺少浏览器时，测试往往是静默跳过而不是报错。看到 `Skipped` 要当红色看，尤其是迁移测试和集成测试——它跳过时，那一整段断言一次都没执行。
- **`--grep` 筛出 0 条也是绿的。** 用例集合根本没加载出来（比如把只当类型用的导入写成了普通具名导入）时，测试框架会安安静静地报告「通过」。改了测试筛选条件后确认跑到的条数对得上。
- **注意「本地绿、CI 红」的系统性差异**：时区（开发机常在东八区，CI 容器多在 UTC）、依赖版本（`npm ci` 装干净的一套 vs 本地现成的 `node_modules`）、浏览器内核、有没有真实的外部服务。和「今天」有关的断言尤其容易在某个时段之后才露馅。
- **渲染性能类的问题以真机为准。** 桌面浏览器的设备仿真只换视口尺寸和 UA，渲染仍走桌面 GPU 和桌面解码器；凡是牵涉合成层、光栅化、图片解码开销的毛病，仿真下一律不复现。
- 行为改动要补或更新**相邻的**测试，先读已有测试再动手，不另建重复的测试体系。
- 收尾前 `git diff --check`，别留空白错误。

## 现象类问题的处理顺序

界面闪烁、卡顿、错位这类问题，关键信息在用户的观察里，不在代码里。**先问清楚现象再动手改**：

1. 什么时候发生（打开时？改设置时？滚动时？）
2. 变的是整块还是局部（比如整块一起白 = 图层问题，只有图片白 = 解码问题）
3. 换个环境还复现吗（真机 / 仿真 / 另一个浏览器）

猜一个成因就改一版，改完让用户试，试完再猜下一个——这样一轮轮试错最费时间，也最容易在代码里留下几层互相打架的补丁。

## 完成标准

- 相关的单元测试、类型检查、端到端测试都真跑过且是绿的。
- 边界情况过一遍：没有数据、外部服务不可用、异步切页、窄屏。不允许页面崩掉或留下没解绑的监听器。
- 最终说明写清楚：做了什么、验证到什么程度、有哪些环境限制、还剩什么风险。
