# 开发规范

跨项目通用，可原样复制到任何仓库。**动手前先读同目录的 `PROJECT.md`**——这里只讲怎么做事，那里才讲这个项目是什么样。

## 代码规范

1. **注释用中文，写「为什么」不写「是什么」。** 能从代码一眼看出来的事不必复述；值得写的是当初踩过的坑、选了这条路的理由、以及不能那样改的原因。分析、说明和 Git 提交信息一并用中文，代码标识符保持仓库现有的英文风格。
2. **核心功能必须有测试。** 判断标准是「坏了会不会有人损失数据或看到错误结果」：数据读写、权限、金额与日期计算、协议解析、离线队列都算核心。纯展示的样式微调不必强求。
3. **改了行为就改相邻的测试。** 先读已有测试再动手，在原来的用例上改，不另建一套重复的测试体系。测试和实现对不上时，先想清楚哪一边错了——有时该改的是测试（需求变了），有时是实现（真的坏了），不要为了变绿而删断言。
4. 小步修改，优先复用已有模块，不为「架构更漂亮」另建同类实现。用补丁方式改文件，不整文件重写。
5. 凭据只从环境变量来，任何真实密钥都不进仓库。

## 工作流

### 1. 需求调研：先问清楚，再动手

不要从一句话需求直接跳到写代码。**改动范围、交互形态、边界情况**这三样但凡有两种合理解释，就先问。

界面上的问题（闪烁、卡顿、错位）尤其如此：关键信息在用户的观察里，不在代码里。问三件事——什么时候发生、变的是整块还是局部、换个环境还复现吗。猜一个成因改一版、让用户试、再猜下一个，是最费时间的做法，也最容易在代码里留下几层互相打架的补丁。

### 2. 文档就位：新项目先立四层结构

```
AGENTS.md     开发规范（本文件，跨项目通用，直接拷）
PROJECT.md    项目上下文：技术栈、代码定位、不可破坏的边界、验证入口、易错点
README.md     对外介绍：这是什么、能做什么
docs/         专项文档，由 PROJECT.md 披露入口
```

新仓库把本文件拷过去，再照着写一份 `PROJECT.md` 即可。

### 3. 开发：遵守上面的代码规范

定位代码先查 `PROJECT.md` 的定位表再用 `rg`，不要漫无目的地遍历目录，永远跳过依赖和产物目录。

拿不准某个 API 或 CSS 属性的真实行为时**当场验一次**，写几十行临时脚本跑一遍就有答案，比凭记忆写完再返工便宜得多。

### 4. 提交前：在本地跑验证

跑 `PROJECT.md` 里定义的验证入口。几条反复踩出来的：

- **改了 UI 就必须真跑一次端到端测试。** 单测和类型检查发现不了「测试还在点一个已经不存在的元素」。
- **跳过等于没测。** 缺 Docker、缺数据库、缺浏览器时测试往往静默跳过而不是报错，看到 `Skipped` 要当红色看。
- **注意「本地绿、CI 红」的系统性差异**：时区、依赖版本、浏览器内核、有没有真实的外部服务。
- **渲染性能类问题以真机为准**，桌面的设备仿真只换视口尺寸，渲染仍走桌面 GPU。

**本地缺环境跑不了的部分，在交付说明里写清楚哪一项没验证、为什么。** 没跑的检查不能写成已通过，跳过的测试不能写成已覆盖。

### 5. 提交：同时更新相关文档

代码改了而文档还停在旧描述，比没有文档更糟——下一个人（或下一次的你）会照着错的做。提交前对一遍：

- 改了架构、目录、验证方式 → 更新 `PROJECT.md`
- 踩了一个「本来可以避免」的坑 → 往 `PROJECT.md` 的易错点里加一条
- 改了对外功能 → 更新 `README.md`
- 改了某个专题的设计决策 → 更新 `docs/` 下对应文档

提交信息用中文，说清楚**为什么这么改**，而不只是改了什么。收尾前 `git diff --check`，别留空白错误。
